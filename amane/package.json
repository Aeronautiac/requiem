{
  "name": "amane",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "description": "requiem's client core: protocol, state model and UI. Hosts inject a transport.",
  "scripts": {
    "check": "svelte-check --tsconfig ./tsconfig.json"
  },
  "devDependencies": {
    "svelte-check": "^4",
    "typescript": "^5"
  },
  "dependencies": {
    "svelte": "^5",
    "tailwindcss": "^4"
  }
}
