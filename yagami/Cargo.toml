[package]
name = "yagami"
version = "0.1.0"
edition = "2024"

[dependencies]
axum = { version = "0.8.9", features = ["ws"] }
futures-util = "0.3.33"
sha2 = "0.11.0"
tokio = { version = "1.53.1", features = [
  "rt-multi-thread",
  "macros",
  "process",
  "io-util",
  "sync",
  "full",
] }
tokio-util = "0.7.19"
lawliet-types = { path = "../lawliet/lawliet-types" }
dotenvy = "0.15.7"
serde_json = "1.0.151"
serde = "1.0.229"
getrandom = "0.4.3"
tower-http = { version = "0.7.0", features = ["cors"] }
enumflags2 = "0.7.12"

[dev-dependencies]
# tests need to mint distinct ActorKey/ViewportKey values by hand; the engine is the only thing
# that ever creates them in production, so this is test scaffolding rather than a real dependency.
slotmap = "1.1.1"
